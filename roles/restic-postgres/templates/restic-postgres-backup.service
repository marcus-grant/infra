[Unit]
Description=Restic backup of PostgreSQL using dump all
After=docker.service

[Service]
Type=simple
ExecStart=/usr/bin/docker run --rm --network={{ restic_postgres_docker_net }} restic-postgres
